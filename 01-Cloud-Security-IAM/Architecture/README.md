# Architecture

This folder contains the architecture diagram for the Cloud Security with AWS IAM project.

The diagram illustrates how IAM users, user groups, IAM policies, Amazon EC2 instances, and resource tags work together to enforce secure access control.
